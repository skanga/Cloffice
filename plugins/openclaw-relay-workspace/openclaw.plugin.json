{
  "id": "relay-workspace",
  "name": "Relay Workspace",
  "description": "Exposes workspace.* Gateway RPC methods so Relay can browse and edit the agent workspace remotely.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "maxListItems": {
        "type": "number",
        "description": "Maximum items returned by workspace.list (default: 200)"
      },
      "maxReadBytes": {
        "type": "number",
        "description": "Maximum file size for workspace.read in bytes (default: 262144 = 256 KB)"
      }
    }
  }
}
